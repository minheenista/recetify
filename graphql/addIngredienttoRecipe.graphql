mutation AddIngredientToRecipe(
  $addingredientInput: addIngredienttoRecipeInput!
) {
  addIngredienttoRecipe(input: $addingredientInput) {
    id
    name
    image {
      url
    }
    pivot {
      quantity
      unit
    }
  }
}