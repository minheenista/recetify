query Comments($comentario:string){
  Comments(comentario:$comentario) {
    paginatorInfo {
      count
      currentPage
      firstItem
      hasMorePages
      lastItem
      lastPage
      perPage
      total
    }
    data {
      id
      comentario
      rating
      Recipe
    }
  }
}
