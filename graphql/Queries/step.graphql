query Step($id: ID ){
  step(id: $id) {
    description
    imagen_path
    Recipe {
      id
      title
      description
      image_pf_path
      origen_food
      time_food
      diet
      prep_time
      calories
      fat
      carbs
      proteins
      rate
      user
      steps
      Comments
    }
  }
}